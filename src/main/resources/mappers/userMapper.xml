<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.swmproject.mappers.userMapper">

	<insert id="signupUser">
		insert into user_info (userid, userpw, username, address, usergender, callnumber)
		values (#{userid}, #{userpw}, #{username}, #{address}, #{usergender}, #{callnumber})		
	</insert>

</mapper>